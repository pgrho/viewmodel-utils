@inherits ListComponentBase<MenuPageItemViewModel>
@using (OnBeginRender())
{
    if (Source != null)
    {
        foreach (MenuPageItemViewModel g in Source)
        {
            <MenuItem @key="@g.GetHashCode()" DataContext="@g" />
        }
    }
}
